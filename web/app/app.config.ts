export default defineAppConfig({
  ui: {
    colors: {
      // Custom ramp defined in app/assets/css/main.css, not a stock Tailwind
      // color — see the comment there for why.
      primary: 'campus',
      secondary: 'teal',
      neutral: 'slate'
    }
  }
})
